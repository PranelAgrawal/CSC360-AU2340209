Reflection:

- Talked about ssh, the fullform of ssh (secure shell) and types of ssh - powershell, command prompt, terminal

Information about ssh:
ssh works with public private key pair
ssh is stored in user directory

Information of ssh keys and cloning the repository using ssh key instead of https. The benefits are that there is no need to authenticate everytime I push into github bcz of ssh key.

- Discussed on what an image is: "It is a matrix where at each cell you have a value, for example if i take photo, the value is rgb, it could also be a single value instead of vector."

- Discussed on what a video is : "Video files are collection of images running at a specific speed, but the actual information is not the speed its the images."

- Discussed about baster vs vector

- Discussed about parallel and perpendicular lines, talked about how to find a perpendicular line if a line is given. To find it, what we can do is find any point on a line which is in the parallel of the given line and connect them. This creates a perpendicular line.

Some questions provided by sir:

1) How is computer graphics different from image processing?

Graphics is generation of images using geometry and image processing is when algorithms is applied on the images to understand those.

2) What are the geometric primitives of drawing?

Lines, curves, areas, etc.

3) Describe the graphical frameworks in java

AWT, swing, javafx , swing was part of runtime till java 21.

4) what is the desing pattern to creating user interfaces, for example buttons?

Event listeners model

5) How is static graphics different from interactive graphics?

Interactive graphics are those wherein a particular portion is changing its state while hovering or clicking, etc

6) How is curve related to calculus?

Curve is somooth continuous differential and used in drawings, areas where we use integration is calculus.

7) Describe ssh and http protocal for accessing git repo.
8) Mention the tools for ssh operation.
9) what are public and private keys?